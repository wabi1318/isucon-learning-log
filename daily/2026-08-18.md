# 2026-08-18

## 今日のテーマと到達目標

- テーマ: Linuxのファイルと権限、Linuxのプロセス
- 資料なしでできるようにすること: 学習ログの現在地と権限を確認し、プログラムと実行中のプロセスの違いを説明する

## 使用した練習環境

- Compose / Lima VM / 使用なし: 使用なし
- 実行した`task`: なし
- 参照したファイル: `../hands-on/linux-files-and-permissions/phase-0.md`、`../hands-on/linux-processes/phase-0.md`

## 始める前にできたこと

- 前日の学習で、ファイルの権限表示を所有者・グループ・その他の利用者に分けて読めた
- 前日の再実行では、学習ログへの移動と確認用コマンドにヒントが必要だった

## 手を動かして確認したこと

| 予想 | 実行したこと | 結果から分かったこと |
|---|---|---|
| 予想は明示せず、前日の操作を資料なしで再現した | 学習ログへ移動し、`pwd`と`ls -ld study-status.md hands-on`を実行した | コマンドをヒントなしで再実行し、現在地と対象の種類・権限を確認できた |
| 権限表示を三組に分ければ各利用者の操作可否を判断できる | 通常のファイルとディレクトリについて、所有者・グループ・その他の利用者ができることを説明した | 通常のファイルは読み書き、ディレクトリは一覧表示・項目の追加や削除・項目への到達として説明する必要がある |
| コードが動くにはNode.jsの実行環境が関係すると考えた | Linuxプロセスの事前問題へ資料なしで回答した | プログラムとプロセスの違い、PID、`ps`、`top`、`pgrep`が次の学習対象だと分かった |
| 一覧、絞り込み、変化を見る目的は異なると考えた | 三つの場面に対して`ps`、`top`、`pgrep`を選び、理由を説明した | `ps`は一覧、`top`は資源使用量の変化、`pgrep`は対象を絞る場合に使う |
| 列名を見れば一つのプロセスを説明できると考えた | 教材用の一行から、利用者、PID、CPU使用率、メモリ使用率、状態、起動内容を読み取った | `STAT`の先頭が`S`であることを根拠に、待機中と判断できた |
| systemdがservice定義に従ってプログラムを起動すると考えた | service、プログラム、プロセス、PIDを対応させ、関係を一文で説明した | systemdが起動を管理し、OSがプロセスを作ってPIDを割り当てる |

## 間違いと原因

- 間違い: ディレクトリの`x`を最初は「実行」と表現し、`r`との違いが曖昧だった
- 間違い: コードが置かれた状態と実行中の状態の違いを「実行環境があるかどうか」と捉え、OSが管理する動作単位までは説明できなかった
- 間違い: PIDをsystemdが付けると捉え、プロセスとPIDを管理するOSの役割が曖昧だった
- 今考えている原因: 同じ記号でも対象によって意味が変わることと、ファイル・実行環境・実行中の動作を別々に捉える経験がまだ少ない
- 次に確かめること: ディレクトリの権限と、systemd、service定義、プログラム、プロセス、PIDの関係をヒントなしで説明できるか

## 資料なしでできたこと

- 学習ログへ移動し、`pwd`と`ls -ld study-status.md hands-on`をヒントなしで実行した
- 権限表示を所有者・グループ・その他の利用者に分けて読んだ
- 指摘後は、ディレクトリの`r`、`w`、`x`を一覧表示・項目の追加や削除・項目への到達として説明した
- コードを動かす際にNode.jsの実行環境が関係すると考えられた
- `ps`、`top`、`pgrep`を、知りたいことに応じて理由付きで選んだ
- 説明を見ながら、プロセス表示の各列を読み、判断根拠を示した
- systemd、service定義、プログラム、プロセス、PIDを対応させ、関係を一文で説明した

## 分からないこと

- ディレクトリの`r`と`x`の違いを、ヒントなしでも安定して説明できるかは翌日に再確認する
- serviceとプロセスの違い、およびOSがPIDを割り当てる点を、翌日にヒントなしで説明できるか再確認する

## 関連する実験

- なし

## 次回の実技問題

- 問題: ディレクトリの`r`、`w`、`x`と、systemdからプロセスが作られる流れを資料なしで説明する
- 成功条件: `r`と`x`を区別し、OSがプロセスへPIDを付けると説明して、`ps`、`top`、`pgrep`を用途へ対応させる
- 復習予定日: 2026-08-19

## 学習時間

- 合計: 未記録
- 内訳: ファイルと権限のPhase 0復習、Linuxプロセスの事前問題、用語確認、調査方法の選択、プロセス表示の読み取り、資料なしの説明を実施。実際の経過時間は未記録
