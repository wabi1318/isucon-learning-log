# 第2週: SQLとインデックス

> [取得元](https://github.com/topi-log/isucon2026-team-sakaguchi/blob/main/plans/catch-up/week-02-sql-index.md) / SHA: `3f8cf059a352aa833c600fb40944c8c9dbd43e91`

- 期間: 2026年8月24日（月）〜8月30日（日）
- 定例: 8月27日（木）
- ゴール: 重いSQLを1件見つけ、実行計画を見てインデックス追加前後を比較できる

## 今週学ぶこと

### リレーショナルDBの基本

- テーブル、列、行、データ型
- 主キーは行を一意に識別する
- 外部キーやID列でテーブル同士を関連付ける
- トランザクションは複数の操作を一まとまりとして扱う
- 正規化はデータの重複や更新時の不整合を減らす考え方

### SQLの基本

- `SELECT`: 列を取得する
- `WHERE`: 行を絞り込む
- `JOIN`: 関連するテーブルを結合する
- `GROUP BY`: 同じ値の行をまとめて集計する
- `ORDER BY`: 結果を並べ替える
- `LIMIT`: 返す件数を制限する

### インデックス

- インデックスは検索用に整列された別のデータ構造
- 読み取りを速くできるが、容量と書き込みコストが増える
- `WHERE`、`JOIN`、`ORDER BY`で使われる列が候補になる
- 複合インデックスでは列の順序が重要
- 推測だけで追加せず、`EXPLAIN`と実行時間で確認する

### ISUCONで頻出する問題

- N+1: 一覧の各行について追加SQLを発行する
- インデックスがなく、毎回テーブル全体を読む
- 必要のない列や行を大量に取得する
- アプリ側でできる集約を何度もDBへ問い合わせる
- ループ内で`INSERT`や`UPDATE`を一件ずつ実行する

## 日別メニュー

### 8/24（月）— 1時間

- テーブル、行、列、主キー、外部キーを学ぶ
- 過去問のテーブルを2つ選び、関連を図にする

### 8/25（火）— 1時間

- 小さなデータで`SELECT`、`WHERE`、`ORDER BY`、`LIMIT`を試す
- 件数を絞る前後で結果がどう変わるか確認する

### 8/26（水）— 1時間

- `JOIN`、`GROUP BY`、`COUNT`を試す
- 定例用に「読めなかったSQL」を1件持ち寄る

### 8/27（木）定例 — 1時間

- 1件のSQLを全員で読み、入力と出力を説明する
- `EXPLAIN`のtype、key、rowsを中心に確認する
- slow query logを有効化する担当と手順を決める

### 8/28（金）— 1時間

- インデックスの仕組みと複合インデックスの列順を学ぶ
- `EXPLAIN`でインデックス追加前後を比較する

### 8/29（土）— 3時間以上

- slow query logを有効化してベンチを1回実行する
- 呼び出し回数と合計時間を含めて、調査対象SQLを1件選ぶ
- SQLを単独実行し、実行時間と`EXPLAIN`を保存する

### 8/30（日）— 3時間以上

- 選んだSQLに対するインデックス案を考える
- インデックスを追加し、SQL実行時間と`EXPLAIN`を比較する
- ベンチを再実行し、スコア・エラー・DB負荷を比較する
- 効果がなければ戻す。効果がない結果も記録する

## 調査テンプレート

```text
対象SQL:
呼び出し元:
呼び出し回数:
合計時間:
変更前のEXPLAIN:
仮説:
変更内容:
変更後のEXPLAIN:
SQL実行時間の変化:
ベンチスコアの変化:
採用 / 不採用:
```

## 成果物

- 主要テーブルの簡単な関連図
- SQL基礎チートシート
- 1件の実行計画とベンチ結果のBefore/After

## 完了条件

- [ ] 基本的な`SELECT`と`JOIN`を読める
- [ ] 主キーとインデックスの役割を説明できる
- [ ] slow query logから調査対象を選べる
- [ ] `EXPLAIN`で利用インデックスと推定行数を確認できる
- [ ] インデックス追加の効果を数値で判定した

## 今週は深追いしないこと

- MySQLオプティマイザの内部実装
- 高度なロック・分離レベル問題
- シャーディングやレプリケーション
- すべてのSQLの改善
