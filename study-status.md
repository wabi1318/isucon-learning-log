# 学習状況

このファイルは現在地の正本です。Codexが実際の学習結果に合わせて更新します。利用者が日程や復習間隔を手で管理する必要はありません。

## 現在地

| 項目 | 状態 |
|---|---|
| 最終更新日 | 2026-08-17 |
| ロードマップ | 第1週: Linuxとサーバーの基礎 |
| 現在のテーマ | Linuxのファイルと権限 |
| 現在のPhase | Phase 0進行中（翌日の再実行待ち） |
| 教材 | `hands-on/linux-files-and-permissions/phase-0.md` |
| 標準学習時間 | 平日60分、土日180分 |

## 練習環境

| 項目 | 状態 |
|---|---|
| 環境名 | Sakaguchi練習環境 |
| リポジトリ | `isucon2026-team-sakaguchi` |
| 標準配置 | 学習ログと同じ親ディレクトリ |
| 今回使う方式 | Phase 0では実環境を使用しない |
| 最後の環境確認 | 2026-08-17（構成ファイルのみ確認） |
| 現在の対象API | なし |
| 現在の参照ファイル | `README.md`、`Taskfile.yml`、`vm/systemd/sakaguchi-app.service`、`vm/provision.sh` |
| 直近のベースライン | なし |

解決した絶対パス、マシン名、SSH情報は記録しない。

## できること

- カレントディレクトリと相対パスから、対象の絶対パスを組み立てられる
- ファイルの権限表示を所有者・グループ・その他の利用者に分け、読み取り・書き込みの可否を判断できる
- ディレクトリの`x`が中の項目への到達を許可すると説明できる
- `ls -ld`を実行し、先頭10文字から種類と操作可否を説明できる
- 練習リポジトリの設定ファイルとスクリプトを`ls -l`で比較し、実行権限の違いを役割から説明できる
- 資料なしで、相対パス、三組の権限、ファイルとディレクトリの`x`、設定ファイルに`x`が不要な理由を説明できる

## 資料なしでは不安なこと

- 学習ログへ移動し、`pwd`と`ls -ld`を実行するまでにヒントが必要
- ディレクトリの`x`を「実行」と表現することがあり、「中の項目への到達」という説明が安定していない

## 期限が来た復習

- なし

詳しい予定は`reviews/queue.md`を正本とする。

## 直近の学習

- [2026-08-17](daily/2026-08-17.md): Phase 0の事前問題で、相対パスから絶対パスを組み立てた

## 関連する実験

- なし

## 次の一手

2026-08-18に、資料を見ずに学習ログへ移動し、`pwd`と`ls -ld`を再実行する。ディレクトリの`x`を「中の項目への到達」と説明できたらPhase 0を完了する。

## 保留事項

- なし
