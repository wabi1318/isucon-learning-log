# テーマ別メモ

何度も見返す知識だけを残します。

例:

- `linux-files-and-permissions.md`
- `mysql-indexes.md`
- `nginx-access-log.md`

日ごとの気付きは、まず`daily/`に残します。何度も参照する内容だけをここへ移します。

1回だけ使ったコマンドや、まだ確かめていない知識は移しません。自分で試し、今後も参照すると判断した内容だけを短く整理します。
